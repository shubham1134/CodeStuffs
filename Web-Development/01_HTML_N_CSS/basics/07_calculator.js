//select the display screen and buttons
const display = document.getElementById("display");
const buttons = document.querySelectorAll("buttons");

// add event
buttons.forEach((button) => {
  click;
});
``